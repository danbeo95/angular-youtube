import { MoveFrameDirective } from './move-frame.directive';

describe('MoveFrameDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveFrameDirective();
    expect(directive).toBeTruthy();
  });
});
